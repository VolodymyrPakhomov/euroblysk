# Скопіюйте цей файл в .env і заповніть реальними значеннями
# .env НЕ комітити в git (вже є в .gitignore)

# Пароль адмін-режиму (має збігатись з URL ?admin=<значення>)
ADMIN_SECRET=тут_має_бути_ваш_пароль
